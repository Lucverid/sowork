# SoWork v1.2.3 — GitHub Pages Build Fix

- Memperbaiki literal `\n` yang tersimpan di `src/style.css` dan membuat Lightning CSS gagal saat production build.
- Menambahkan `gh-pages`.
- Menambahkan script `npm run deploy`.
- Menambahkan `vite.config.js`: localhost tetap `/`, production build memakai base `/sowork/`.
- Target repo GitHub Pages: `https://lucverid.github.io/sowork/`.
